<% if @image.new_record? %>
  alert("Failed to upload image: <%= j @image.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#images").append("<%= j render(:partial => 'galleries/image', :object =>@image, :as => 'image') %>");
<% end %>