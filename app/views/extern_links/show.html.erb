<% content_for :title do %>
  <%= t ".title", :name => @extern_link.link.name %>
<% end %>

<%# content_for :buttons do %>
  <%= link_to links_path, :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    <%= t "general.list" %>
  <% end %>
  <% if can? :update, @extern_link %>
    <%= link_to edit_extern_link_path(@extern_link), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
      <%= t "general.edit" %>
    <% end %>
  <% end %>
<%# end %>

<dl class="dl-horizontal">
  <dt><%= ExternLink.human_attribute_name("url") %>:</dt>
  <dd><%= @extern_link.url %></dd>

</dl>
