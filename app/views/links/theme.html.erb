<div class="window box">
  <%= simple_form_for(@link, :defaults => { :input_html => { :class => 'input-xlarge' } }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :description, :as => :text %>
      <%= f.input :color, :input_html => { :class => "colorpicker", :data => {:"color-format" => "hex"}} %>
      <%= f.simple_fields_for :banner do |image_form| %>
        <%#= image_form.input :file, :as => :file %>
        <%= render 'images/image_selector', f:image_form, image:@link.banner %>
      <% end %>
    </div>

    <div class="form-group">
      <div class="col-lg-offset-3 col-lg-9">
        <%= f.button :submit, :class => "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
