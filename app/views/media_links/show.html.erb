<% content_for :title do %>
  <%= t ".title", :name => @media_link.name %>
<% end %>

<%# content_for :buttons do %>
  <%= link_to links_path, :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    <%= t "general.list" %>
  <% end %>
  <% if can? :update, @media_link %>
    <%= link_to edit_media_link_path(@media_link), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
      <%= t "general.edit" %>
    <% end %>
  <% end %>
<%# end %>

<dl class="dl-horizontal">
  <dt><%= MediaLink.human_attribute_name("controller_id") %>:</dt>
  <dd><%= @media_link.controller_id %></dd>

  <dt><%= MediaLink.human_attribute_name("instance_id") %>:</dt>
  <dd><%= @media_link.instance_id %></dd>

</dl>
